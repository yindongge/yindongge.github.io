/*package com.org.manage.web.controller;

import java.util.List;

import javax.annotation.Resource;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import com.org.baseserver.entity.user.QrcodeUser;
import com.org.baseserver.service.user.QrcodeUserService;

@Controller
@RequestMapping("users")
public class userController {
	@Resource
	private QrcodeUserService  qrcodeUserService;
	@RequestMapping(value ="/list", method = { RequestMethod.GET, RequestMethod.POST })
	public String list(Model  model,QrcodeUser query){		
		 List<QrcodeUser> userlist=qrcodeUserService.queryList(query);
		 model.addAttribute("userlist", userlist);
		return "user/user";
		
	}
}
*/